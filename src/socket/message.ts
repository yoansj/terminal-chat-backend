// import { FastifyInstance } from "fastify";
// import { Server, Socket } from "socket.io";

// interface Params {
//   socket: Socket;
//   io: Server;
//   fastify: FastifyInstance;
// }

// export default function MessageManager({ socket, io, fastify }: Params) {
//   socket.on("message", (message: string) => {
//     socket.to()
//   })
// }
